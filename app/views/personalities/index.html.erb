<h1>Listing personalities</h1>

<table>
  <thead>
    <tr>
      <th>Users</th>
      <th>Red</th>
      <th>Yellow</th>
      <th>Green</th>
      <th>Blue</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @personalities.each do |personality| %>
      <tr>
        <td><%= personality.users_id %></td>
        <td><%= personality.red %></td>
        <td><%= personality.yellow %></td>
        <td><%= personality.green %></td>
        <td><%= personality.blue %></td>
        <td><%= link_to 'Show', personality %></td>
        <td><%= link_to 'Edit', edit_personality_path(personality) %></td>
        <td><%= link_to 'Destroy', personality, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Personality', new_personality_path %>
