<%= form_for(@workshop_score) do |f| %>
  <% if @workshop_score.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@workshop_score.errors.count, "error") %> prohibited this workshop_score from being saved:</h2>

      <ul>
      <% @workshop_score.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :users_id %><br>
    <%= f.number_field :users_id %>
  </div>
  <div class="field">
    <%= f.label :workshops_id %><br>
    <%= f.number_field :workshops_id %>
  </div>
  <div class="field">
    <%= f.label :score %><br>
    <%= f.text_field :score %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
